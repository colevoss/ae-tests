components:
  schemas:
    Playlist:
      type: object
      properties:
        id:
          type: number
        name:
          type: string
        userId:
          type: string
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    CreatePlaylistPayload:
      type: object
      properties:
        name:
          type: string
        userId:
          type: string